# pyproject.toml

[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pim-bayesiannetwork"
version = "0.0.1"
license = { file = "LICENSE" }
dependencies = [
  'torch'
]
